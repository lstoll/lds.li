# fly.toml app configuration file generated for lds-li on 2025-09-30T23:47:23+02:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#


# flyctl secrets set EMAIL_ADDRESS=xxxx
# flyctl certs add lds.li
# flyctl certs add www.lds.li

app = 'lds-li'
primary_region = 'iad'

[build]
  # [build.args]
  #   GO_VERSION = '1.25.1'

[env]
  PORT = '8080'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  size = 'shared-cpu-1x'
